/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/TaxAIDD',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
